from src.custom_functionality import UserInteractive  # Импорт класса UserInteractive из модуля custom_functionality

# Основной блок программы, который запускается при выполнении данного файла
if __name__ == "__main__":
    """
    Основной блок программы, запускающий взаимодействие с пользователем.

    Здесь происходит вызов метода user_interaction класса UserInteractive, который
    отвечает за весь процесс взаимодействия с пользователем.
    """
    UserInteractive.user_interaction(self=None)  # Вызываем метод user_interaction класса UserInteractive
